using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class MainMenuPanelButton : MonoBehaviour
{
    [SerializeField] GameObject tabObj;

 //   private void OnEnable() => MainMenu.OnPanelButtonClicked += OpenCloseTabObject;
  //  private void OnDisable() => MainMenu.OnPanelButtonClicked -= OpenCloseTabObject;

   // private void OpenCloseTabObject(Button button) => tabObj.SetActive(button == GetComponent<Button>());
   
}
