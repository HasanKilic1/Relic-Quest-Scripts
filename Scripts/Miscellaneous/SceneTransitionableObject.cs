using UnityEngine;

public class SceneTransitionableObject : MonoBehaviour
{/*
    public enum InitializeMethod
    {
        Awake,
        Start
    }
    [SerializeField] InitializeMethod initializeMethod;
    private void Awake()
    {
        if(initializeMethod == InitializeMethod.Awake)
        {
            DontDestroy.Instance.MakeObjectUndestroyable(transform);
        }
    }
    void Start()
    {
        if(initializeMethod == InitializeMethod.Start)
        {
            DontDestroy.Instance.MakeObjectUndestroyable(transform);
        }
    }
    */
}
